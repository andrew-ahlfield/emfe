# EM Frequency Explorer (emfe) — environment template.
# Copy to .env and fill in. .env is git-ignored; this file is the committed template.

# FCC Spectrum Dashboard API.
# Get a free universal US federal data key at: https://api.data.gov/signup/
# (The proxy sends this key only when present, and falls back to a committed
#  snapshot if the upstream API is unavailable.)
FCC_API_KEY=
